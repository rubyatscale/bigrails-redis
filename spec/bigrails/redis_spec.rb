# frozen_string_literal: true

RSpec.describe BigRails::Redis do
  it "has a version number" do
    expect(BigRails::Redis::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
